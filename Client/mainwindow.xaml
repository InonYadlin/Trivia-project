<Window x:Class="SimpleLoginApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="500" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Username:" Grid.Row="0" Grid.Column="0" Margin="10"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <Label Content="Password:" Grid.Row="1" Grid.Column="0" Margin="10"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="10"/>

        <Button Content="Login" Grid.Row="2" Grid.Column="1" Margin="34,29,0,282" HorizontalAlignment="Left" Click="btnLogin_Click" Grid.RowSpan="2" Width="258" RenderTransformOrigin="0.363,-0.569"/>


        <Button Content="Don't have an account? Sign up!" Grid.Row="3" Grid.Column="1" Margin="0,150,122,162" HorizontalAlignment="Right" FontSize="14" Click="btnSignUp_Click" Width="258"/>
    </Grid>
</Window>