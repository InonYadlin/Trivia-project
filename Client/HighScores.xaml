<Window x:Class="HighScoresApp.HighScoresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="High Scores" Height="177" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="User 1:" Grid.Row="0" Grid.Column="0" Margin="10" FontSize="14"/>
        <TextBlock x:Name="User1Value" Grid.Row="0" Grid.Column="1" Margin="10" FontSize="14"/>

        <Label Content="User 2:" Grid.Row="1" Grid.Column="0" Margin="10" FontSize="14"/>
        <TextBlock x:Name="User2Value" Grid.Row="1" Grid.Column="1" Margin="10" FontSize="14"/>

        <Label Content="User 3:" Grid.Row="2" Grid.Column="0" Margin="10" FontSize="14"/>
        <TextBlock x:Name="User3Value" Grid.Row="2" Grid.Column="1" Margin="10" FontSize="14"/>
    </Grid>
</Window>
