<Window x:Class="WpfApp1.WaitingRoomToJoin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WaitingRoomToJoin">

    <Grid>
        <!-- Content of the Waiting Room -->
        <TextBlock Text="Please wait, you are in the waiting room..." 
                   VerticalAlignment="Top" HorizontalAlignment="Left" 
                   Foreground="Aqua"
                   FontSize="25" Margin="206,137,0,0"/>

        <!-- Cancel Button at the Bottom Right -->
        <Button Content="Cancel (Leave room)" Width="150" Height="30" 
                VerticalAlignment="Top" HorizontalAlignment="Center" 
                Margin="0,350,0,0" Click="CancelButton_Click" RenderTransformOrigin="-0.171,0.613"/>

        <Grid.Background>
            <ImageBrush ImageSource=".\Resources\background.jpg"/> 
        </Grid.Background> 
    </Grid>

</Window>
