<Window x:Class="WpfApp1.waitingRoomForCreate"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="waitingRoomForCreate">

    <Grid>
        <!-- Content of the Waiting Room -->
        <TextBlock Text="Please wait, you are in the waiting room..." 
               VerticalAlignment="Top" HorizontalAlignment="Left" 
               Foreground="Aqua"
               FontSize="25" Margin="206,137,0,0"/>

        <!-- Start Button at the Bottom Right -->
        <Button Content="Start game!" Width="150" Height="30" 
            VerticalAlignment="Bottom" HorizontalAlignment="Right" 
            Margin="0,0,86,72" Click="StartButton_Click"/>
        
        <!-- Cancel Button at the Bottom Left -->
        <Button Content="Cancel (Close room)" Width="150" Height="30" 
            VerticalAlignment="Bottom" HorizontalAlignment="Right" 
            Margin="0,0,500,72" Click="CancelButton_Click"/>
        <Grid.Background>
            <ImageBrush ImageSource=".\Resources\background.jpg"/>
        </Grid.Background>
    </Grid>

</Window>
